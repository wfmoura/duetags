# API de Etiquetas Personalizadas

Esta API gerencia o salvamento, listagem e download de etiquetas personalizadas.

## Como rodar

1. Instale as dependências:
   ```bash
   npm install
   ```

2. Inicie o servidor:
   ```bash
   npm run dev
   ```

A API estará disponível em `http://localhost:3001`.
